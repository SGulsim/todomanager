services:
  - type: web
    name: todomanager-backend
    env: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: PORT
        value: 8080
      - key: HOST
        value: 0.0.0.0
      - key: DB_PATH
        value: ./data/tasks.db

