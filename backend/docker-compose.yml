version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - HOST=0.0.0.0
      - DB_PATH=./data/tasks.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped

