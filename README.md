# 📋 Менеджер задач (Todo Manager)

Полнофункциональное веб-приложение для управления задачами с серверной частью на C++ и современным React-интерфейсом.

## 📖 Описание проекта

Todo Manager — это full-stack веб-приложение для управления персональными задачами. Проект демонстрирует использование C++ для создания REST API сервера в сочетании с современным React-фронтендом. Приложение позволяет пользователям эффективно организовывать свои задачи, устанавливать приоритеты, отслеживать сроки выполнения и управлять своим рабочим процессом.

## ✨ Основные возможности

### 🔐 Аутентификация и безопасность
- Регистрация новых пользователей
- Безопасный вход в систему
- JWT-токены для авторизации
- Хеширование паролей (bcrypt)
- Защита API endpoints

### 📝 Управление задачами
- **Создание задач** с названием, описанием и приоритетом
- **Редактирование** существующих задач
- **Удаление** задач с подтверждением
- **Отметка выполнения** задач (чекбокс)
- **Установка сроков выполнения** (дедлайнов)
- **Установка даты создания** задачи
- **Приоритеты**: Высокий, Средний, Низкий

### 🔍 Фильтрация и поиск
- Фильтрация по статусу (Все / Невыполненные / Выполненные)
- Фильтрация по датам:
  - Просроченные задачи
  - Задачи на сегодня
  - Задачи на этой неделе
  - Задачи без срока
- Поиск по названию задачи

### 📊 Сортировка
- **По приоритету** (по умолчанию):
  - Высокий → Низкий
  - Низкий → Высокий
- **По дате создания** (новые/старые)
- **По названию** (алфавитно)

### 🎨 Визуальные индикаторы
- Цветовая индикация приоритетов:
  - 🔴 Высокий приоритет (красный)
  - 🟡 Средний приоритет (оранжевый)
  - 🟢 Низкий приоритет (зеленый)
- Статусы задач с визуальными бейджами
- Автоматическое выделение просроченных задач
- Адаптивный дизайн

### 📅 Умная логика дат
- Автоматическое определение просроченных задач
- Учет даты создания при определении просрочки:
  - Если дата создания в прошлом, но дедлайн еще не наступил → задача не просрочена
  - Если дедлайн прошел → задача помечается как просроченная
- Отображение даты создания и срока выполнения

### 📈 Статистика
- Общее количество задач
- Количество выполненных задач
- Количество задач в ожидании

## 🛠 Технологический стек

### Backend
- **Язык**: C++17
- **HTTP сервер**: [cpp-httplib](https://github.com/yhirose/cpp-httplib)
- **База данных**: SQLite3
- **Сборка**: CMake 3.15+
- **Аутентификация**: JWT токены
- **Хеширование**: bcrypt для паролей

### Frontend
- **Фреймворк**: React 18.2
- **Сборщик**: Vite 5.0
- **HTTP клиент**: Axios 1.6
- **Стили**: CSS3 (модульные стили)
- **Язык**: JavaScript (ES6+)

### Инфраструктура
- **База данных**: SQLite (файловая БД)
- **Прокси**: Vite Dev Server Proxy
- **Порты**: 
  - Backend: `8080`
  - Frontend: `3000`

## 📋 Требования

### Для Backend
- **Компилятор C++17**:
  - Windows: Visual Studio 2022 или MinGW
  - Linux: GCC 7+ или Clang 5+
  - macOS: Xcode 10+
- **CMake** 3.15 или выше
- **SQLite3** библиотека
- **Git** (для клонирования репозитория)

### Для Frontend
- **Node.js** 16.0 или выше
- **npm** 7.0 или выше (или yarn/pnpm)

## 🚀 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd todomanager
```

### 2. Сборка Backend

#### Windows (Visual Studio)

```bash
cd backend
mkdir build
cd build
cmake .. -G "Visual Studio 17 2022" -A x64
cmake --build . --config Release
```

#### Linux/macOS

```bash
cd backend
mkdir build
cd build
cmake ..
cmake --build . --config Release
```

После успешной сборки исполняемый файл будет находиться в:
- Windows: `backend/build/Release/todomanager.exe`
- Linux/macOS: `backend/build/todomanager`

### 3. Установка Frontend

```bash
cd frontend
npm install
```

### 4. Запуск приложения

#### Терминал 1 - Backend сервер

**Windows:**
```bash
cd backend/build/Release
.\todomanager.exe
```

**Linux/macOS:**
```bash
cd backend/build
./todomanager
```

Сервер запустится на `http://localhost:8080`

#### Терминал 2 - Frontend сервер

```bash
cd frontend
npm run dev
```

Frontend будет доступен на `http://localhost:3000`

### 5. Открытие в браузере

Откройте браузер и перейдите по адресу: **http://localhost:3000**

## 📁 Структура проекта

```
todomanager/
├── backend/                    # C++ серверная часть
│   ├── src/                   # Исходные файлы
│   │   ├── main.cpp          # Точка входа сервера
│   │   ├── routes.cpp        # HTTP маршруты и обработчики
│   │   ├── db.cpp            # Работа с базой данных
│   │   ├── auth.cpp          # Аутентификация и авторизация
│   │   ├── task.cpp          # Логика работы с задачами
│   │   └── user.cpp          # Логика работы с пользователями
│   ├── include/              # Заголовочные файлы
│   │   ├── routes.h
│   │   ├── db.h
│   │   ├── auth.h
│   │   ├── task.h
│   │   └── user.h
│   ├── third_party/          # Сторонние библиотеки
│   │   └── httplib.h         # cpp-httplib
│   ├── build/                # Собранные файлы
│   │   ├── Release/          # Release сборка
│   │   │   ├── todomanager.exe
│   │   │   └── sqlite3.dll
│   │   └── data/            # База данных SQLite
│   │       └── tasks.db
│   └── CMakeLists.txt        # Конфигурация CMake
│
├── frontend/                  # React клиентская часть
│   ├── src/
│   │   ├── components/       # React компоненты
│   │   │   ├── Login.jsx    # Компонент входа/регистрации
│   │   │   ├── TaskList.jsx # Список задач с фильтрами
│   │   │   ├── TaskItem.jsx # Элемент задачи
│   │   │   └── TaskForm.jsx # Форма создания/редактирования
│   │   ├── services/         # API клиент
│   │   │   └── api.js       # Axios конфигурация и методы
│   │   ├── utils/           # Утилиты
│   │   │   └── dateUtils.js # Работа с датами
│   │   ├── App.jsx          # Главный компонент
│   │   └── index.jsx        # Точка входа
│   ├── public/              # Статические файлы
│   ├── dist/                # Production сборка
│   ├── package.json         # Зависимости и скрипты
│   └── vite.config.js       # Конфигурация Vite
│
├── scripts/                  # Вспомогательные скрипты
│   └── generateProjectInfo.js
│
└── README.md                 # Документация
```

## 🔌 API Документация

### Базовый URL
```
http://localhost:8080/api
```

### Аутентификация

Все запросы к API (кроме регистрации и входа) требуют заголовок:
```
Authorization: Bearer <token>
```

### Endpoints

#### Регистрация
```http
POST /api/auth/register
Content-Type: application/json

{
  "username": "string",
  "password": "string"
}
```

**Ответ:**
```json
{
  "message": "User created successfully"
}
```

#### Вход
```http
POST /api/auth/login
Content-Type: application/json

{
  "username": "string",
  "password": "string"
}
```

**Ответ:**
```json
{
  "token": "jwt_token",
  "user_id": 1,
  "username": "string"
}
```

#### Получить все задачи
```http
GET /api/tasks
Authorization: Bearer <token>
```

**Ответ:**
```json
[
  {
    "id": 1,
    "user_id": 1,
    "title": "Название задачи",
    "description": "Описание задачи",
    "due_date": "2024-12-31",
    "priority": "high",
    "status": "pending",
    "created_at": "2024-01-01 12:00:00",
    "updated_at": "2024-01-01 12:00:00"
  }
]
```

#### Создать задачу
```http
POST /api/tasks
Authorization: Bearer <token>
Content-Type: application/json

{
  "title": "string",
  "description": "string",
  "due_date": "2024-12-31",
  "created_at": "2024-01-01",
  "priority": "high|medium|low"
}
```

**Ответ:** Созданная задача (JSON объект)

#### Обновить задачу
```http
PUT /api/tasks/:id
Authorization: Bearer <token>
Content-Type: application/json

{
  "title": "string",
  "description": "string",
  "due_date": "2024-12-31",
  "priority": "high|medium|low",
  "status": "pending|in_progress|completed"
}
```

**Ответ:** Обновленная задача (JSON объект)

#### Удалить задачу
```http
DELETE /api/tasks/:id
Authorization: Bearer <token>
```

**Ответ:**
```json
{
  "message": "Task deleted successfully"
}
```

### Коды ответов

- `200` - Успешный запрос
- `201` - Ресурс создан
- `400` - Неверный запрос
- `401` - Не авторизован
- `403` - Доступ запрещен
- `404` - Ресурс не найден
- `409` - Конфликт (например, пользователь уже существует)
- `500` - Внутренняя ошибка сервера

## 💻 Разработка

### Запуск в режиме разработки

**Frontend (с hot-reload):**
```bash
cd frontend
npm run dev
```

**Backend:**
Пересоберите проект в Debug конфигурации для отладки:
```bash
cd backend/build
cmake --build . --config Debug
```

### Production сборка

**Frontend:**
```bash
cd frontend
npm run build
```

Собранные файлы будут в `frontend/dist/`

**Backend:**
```bash
cd backend/build
cmake --build . --config Release
```

### База данных

База данных SQLite автоматически создается при первом запуске сервера в директории `backend/build/data/tasks.db`.

**Схема базы данных:**

**Таблица `users`:**
- `id` (INTEGER PRIMARY KEY)
- `username` (TEXT UNIQUE)
- `password_hash` (TEXT)
- `created_at` (TEXT)

**Таблица `tasks`:**
- `id` (INTEGER PRIMARY KEY)
- `user_id` (INTEGER, FOREIGN KEY)
- `title` (TEXT)
- `description` (TEXT)
- `due_date` (TEXT)
- `priority` (TEXT)
- `status` (TEXT)
- `created_at` (TEXT)
- `updated_at` (TEXT)

## 🎯 Особенности реализации

### Backend
- RESTful API архитектура
- Подготовленные SQL запросы для защиты от SQL-инъекций
- JWT токены для безопасной аутентификации
- Хеширование паролей с использованием bcrypt
- Валидация входных данных
- Обработка ошибок и исключений
- CORS поддержка для фронтенда

### Frontend
- Компонентная архитектура React
- Управление состоянием через React Hooks
- Мемоизация для оптимизации производительности
- Адаптивный дизайн
- Обработка ошибок API
- Локальное хранение токенов
- Автоматический редирект при истечении сессии

## 📝 Лицензия

Учебный проект для демонстрации веб-разработки на C++ и React.

## 👥 Авторы

Проект создан в образовательных целях для изучения full-stack разработки.
Саралиева Гульсим и Рязанова Мария. К0709-23/3

**Приятного использования!**
